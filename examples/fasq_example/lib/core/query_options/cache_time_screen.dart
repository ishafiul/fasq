import 'package:flutter/material.dart';
import '../../widgets/example_scaffold.dart';

class CacheTimeScreen extends StatelessWidget {
  const CacheTimeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return ExampleScaffold(
      title: 'Cache Time',
      description: 'Coming soon - Show cache retention after unmounting.',
      child: const Center(
        child: Text('This example will be implemented soon'),
      ),
    );
  }
}

